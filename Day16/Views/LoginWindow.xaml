<Window x:Class="StudentDiaryFull.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Электронный дневник — Вход"
        Height="440" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F0F4F8">
    <Border Margin="28" Background="White" CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect Color="#C0C0C0" BlurRadius="18" ShadowDepth="2" Opacity="0.25"/>
        </Border.Effect>
        <StackPanel Margin="28" VerticalAlignment="Center">
            <TextBlock Text="📚" FontSize="44" HorizontalAlignment="Center" Margin="0,0,0,6"/>
            <TextBlock Text="Электронный дневник" FontSize="19" FontWeight="Bold"
                       Foreground="#2C3E50" HorizontalAlignment="Center"/>
            <TextBlock Text="Войдите в систему" FontSize="12" Foreground="#7F8C8D"
                       HorizontalAlignment="Center" Margin="0,4,0,20"/>
            <TextBlock Text="Логин" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,4"/>
            <TextBox x:Name="LoginBox" Padding="10,7" FontSize="13"
                     BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,0,12"/>
            <TextBlock Text="Пароль" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PwdBox" Padding="10,7" FontSize="13"
                         BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,0,6"/>
            <TextBlock FontSize="10" Foreground="#95A5A6" Margin="0,0,0,14">
                Преподаватель: teacher / teacher123&#x0a;Студент: petrov / 1234
            </TextBlock>
            <TextBlock x:Name="ErrorLabel" Foreground="#E74C3C" FontSize="12"
                       Visibility="Collapsed" Margin="0,0,0,10" TextWrapping="Wrap"
                       HorizontalAlignment="Center"/>
            <Button x:Name="LoginBtn" Content="Войти" Height="40"
                    FontSize="14" FontWeight="SemiBold"
                    Background="#3498DB" Foreground="White"
                    BorderThickness="0" Cursor="Hand" Click="LoginBtn_Click"/>
        </StackPanel>
    </Border>
</Window>
