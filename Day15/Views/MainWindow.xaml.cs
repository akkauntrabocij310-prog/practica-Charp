using System.Windows;

namespace StudentDiary.Views
{
    /// <summary>
    /// Code-behind намеренно пустой — вся логика в ViewModel (MVVM).
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
